name: Test Workflow

on:
  push:
    paths:
      - 'jobs/**/*.md'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run script
        run: python tailor.py